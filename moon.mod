name = "Omochice/random_cron"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/Omochice/random-cron"

license = "Apache-2.0"

keywords = [ ]

description = "Generate a random cron expression for a schedule"

preferred_target = "native"